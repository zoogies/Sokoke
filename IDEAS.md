# IDEAS

- besides debug overlay text, we should be HTML-canvas first UI rendering. It just makes UI design so much easier with CSS for styling. unsure if react or other is possible but as long as its not too much bloat it would be probably really nice
