# Runtime TODO

The runtime is the program (with a `main`) that loads the game dll and executes it. This can be ignored until our manual testbed runtime exists and works, and could likely be copied over and made generic.
